"""Configuration parsing for .ww files.

This package provides the tokenizer, resolver, factory, parser, and operations
modules that together handle parsing .ww configuration into running components.
"""
