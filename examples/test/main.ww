classifier = model pytorch {
    model_weights: "model.pt"
    model_code: "model.Classifier"
}

llm = decomposer llm {
    api_key: $OPENAI_API_KEY
    tools: [classifier]
}

in = input command_line {
    to: llm
}
