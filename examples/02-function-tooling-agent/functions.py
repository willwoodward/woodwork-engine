def generate_random_word():
    """A function that returns a string of random characters excluding whitespaces."""

    return "jfdrihsjdofjesoijesiofj"


def word_length(word: str):
    """A function that takes a string as input and returns its length as an integer."""

    return len(word)


def sort_list(list: list[any]):
    """A function that takes a list and returns the sorted list."""

    return sorted(list)
